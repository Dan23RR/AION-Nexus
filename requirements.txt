# AION-NEXUS production runtime dependencies
# Pinned to known-working versions for reproducibility.

# Core ML
torch>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0
scipy>=1.10.0,<2.0.0          # required for HP-Butterworth filter in preprocessing

# Verification layer
# Upper bound is the next un-tested major. 47.0.0 is the version we test and ship
# against; <48 prevents a silent pull of an unvetted major into the signing path.
cryptography>=43,<48        # Ed25519 asymmetric signing in aion_nexus.verify (third-party-checkable certificates)

# Server
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
pydantic>=2.0.0,<3.0.0
python-multipart>=0.0.9,<1.0.0
prometheus-client>=0.20.0,<1.0.0   # /metrics endpoint (Prometheus exposition format)

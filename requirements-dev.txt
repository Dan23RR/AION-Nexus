# Development extras (pip install -r requirements-dev.txt)
-r requirements.txt

pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.4.0
mypy>=1.10.0

# Packaging / release checks (CI: python -m build + twine check)
build>=1.2.0
twine>=5.0.0

# Supply-chain audit (scripts/audit_supply_chain.py wraps pip-audit)
pip-audit>=2.7.0

# Verification helpers
scikit-learn>=1.3.0
pandas>=2.0.0

# ONNX export
onnx>=1.16.0
onnxruntime>=1.17.0

# Optional client tooling for examples
requests>=2.31.0

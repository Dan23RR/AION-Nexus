# AION-NEXUS production runtime dependencies
# Pinned to known-working versions for reproducibility.

# Core ML
torch>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0
scipy>=1.10.0,<2.0.0          # required for HP-Butterworth filter in preprocessing

# Verification layer
cryptography>=43             # Ed25519 asymmetric signing in aion_nexus.verify (third-party-checkable certificates)

# Server
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
pydantic>=2.0.0,<3.0.0
python-multipart>=0.0.9
prometheus-client>=0.20.0,<1.0.0   # /metrics endpoint (Prometheus exposition format)
